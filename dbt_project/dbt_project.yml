name: 'crm_dbt'
version: '1.0.0'
config-version: 2

profile: 'crm_dbt'

model-paths: ["models"]
test-paths:  ["tests"]
macro-paths: ["macros"]

target-path:   "target"
clean-targets: ["target", "dbt_packages"]

models:
  crm_dbt:
    staging:
      +materialized: view
      +schema: staging